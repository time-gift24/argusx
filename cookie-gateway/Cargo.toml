[package]
name = "cookie-gateway"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true, features = ["derive"] }
tokio = { workspace = true, features = ["rt-multi-thread", "macros", "sync"] }
async-trait = "0.1"
tracing = "0.1"

[dev-dependencies]
tokio-test = "0.4"
